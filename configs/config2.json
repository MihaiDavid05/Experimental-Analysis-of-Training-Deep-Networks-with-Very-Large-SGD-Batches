{
  "name": "",
  "data": "data",
  "log_dir": "logs",
  "model": "vgg16cifar",
  "dataset": "cifar10",
  "batch_size": 128,
  "epochs": 100,
  "learning_rate": 0.1,
  "early_stopping": 0,
  "early_stopping_patience": 22,
  "use_lr_scheduler": "Step",
  "optimizer": "sgd",
  "checkpoint_dir": "checkpoints",
  "augmentations": 1
}